---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.title == 'blank' %}
        <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
        {{ site.last_name }}
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>
    <p class="desc">{{ page.subtitle }}</p>
  </header>

  <article>
    {% if page.profile %}
      <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% if page.profile.image_circular %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
          {% else %}
            {% assign profile_image_class = 'img-fluid z-depth-1
      rounded' %}
          {% endif %}
          {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
      30vw, 95vw"{% endcapture %}
          {%
            include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
            cache_bust=true
          %}
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
      </div>
    {% endif %}
    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.news and site.announcements.enabled %}
      <h2>🎉 news</h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Work -->
    {% if page.work and site.announcements.enabled %}
      <h2>
        <a href="{{ '/work/' | relative_url }}" style="color: inherit">💼 work</a>
      </h2>
      {% include work.liquid limit=true %}
    {% endif %}

    <!-- Education -->
    {% if page.education and site.announcements.enabled %}
      <h2>
        <a href="{{ '/education/' | relative_url }}" style="color: inherit">🎓 education</a>
      </h2>
      {% include education.liquid limit=true %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/papers/' | relative_url }}" style="color: inherit">📝 selected papers</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- extracurriculars -->
    <h2>
      <a href="{{ '/extracurriculars/' | relative_url }}" style="color: inherit">🥁 drumming</a>
    </h2>

    <p>
      Drumming has been a consistent passion in my life, helping me maintain creativity and rhythm in everything I do. Check out my
      <a href="{{ '/extracurriculars/' | relative_url }}">videos</a> here, or visit my
      <a href="https://youtube.com/@AllenG-y9z" target="_blank" rel="noopener">YouTube</a> and
      <a href="https://space.bilibili.com/402007358" target="_blank" rel="noopener">Bilibili</a> channels.
    </p>
    <div class="row mt-3">
      <div class="col-sm-4 mt-3">
        {% include video.liquid path="https://www.youtube.com/embed/vrtRl6gWNWA" class="img-fluid rounded z-depth-1" %}
        <div class="caption">Linkin Park - Numb</div>
      </div>
      <div class="col-sm-4 mt-3">
        {% include video.liquid path="https://www.youtube.com/embed/JxF-EMoV-n4" class="img-fluid rounded z-depth-1" %}
        <div class="caption">Bring Me The Horizon - Avalanche (Drum Cover)</div>
      </div>
      <div class="col-sm-4 mt-3">
        {% include video.liquid path="https://www.youtube.com/embed/R5GD8QqwopY" class="img-fluid rounded z-depth-1" %}
        <div class="caption">LiSA - 紅蓮華 (Drum Cover)</div>
      </div>
    </div>

    <!-- Latest posts -->
    {% if site.latest_posts.enabled %}
      <div style="display: flex;">
        <div style="flex: 0 0 60%; padding: 10px; margin-right: 20px;">
          {% if site.latest_posts.enabled %}
            <h2>
              <a href="{{ '/blog/' | relative_url }}" style="color: inherit">latest posts</a>
            </h2>
            {% include latest_posts.liquid %}
          {% endif %}
        </div>
        <div style="flex: 0 0 40%; padding: 10px;">
          <script
            type="text/javascript"
            src="//rf.revolvermaps.com/0/0/7.js?i=569ba6lplw3&amp;m=0&amp;c=fff600&amp;cr1=ff0000&amp;sx=0"
            async="async"
          ></script>
        </div>
      </div>
    {% endif %}
  </article>
</div>
